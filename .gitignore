.DS_Store
vendor/
includes/db_config.php.DS_Store
xampp-php-project/
vendor/
Do-not-upload-to-server/
includes/db_config.php.DS_Store

# Local Python dependency cache (vendored Pillow, etc.) — never deploy
.pydeps/

# Personal finance utility; kept local, not part of the public site
tools/fidelity_cma_to_ynab.py

# Cursor IDE local rules/state — never commit
.cursor/

# Phase 4 calibration — private Plan G Phase 3 values (never commit)
journey.ronbelisle.com/dev/phase-4-calibration/plan-g.local.json

# Local release archives and staging output.
release-artifacts/
