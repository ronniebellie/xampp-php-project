# Consumer audit, September 2026

## Batch 1 — snapshot income timing

The independently reproduced age-67 primary / age-57 spouse scenario previously paid the spouse ten years before their age-67 claim, showing $36,000 household Social Security and a $350,000 target instead of $12,000 and $950,000. Spouse PIA also bypassed claiming adjustments. The snapshot now maps each spouse to their own age and birth cohort and uses `RBFinance.calculateMonthlyBenefit` for both own-record benefits. IRA beneficiary status no longer hides or discards the spouse age needed for this calculation.

The target and comparison balance use retirement-start amounts, not a later withdrawal date or retirement-year ending balance. Later other income has an explicit start age. The screen and report separate income available this year, at retirement, after later income begins, and bridge-period withdrawals/shortfalls. Future income cannot reduce earlier spending needs. Deep links preserve a real zero and use currently available income rather than future PIA; birth-date links preserve actual dates. Invalid reruns clear stale results.

The timing contract is deliberately annual: ages attained in the calendar year; retirement/claims at the start of their selected modeled age-year; twelve monthly payments per year. Claim ages and retirement horizons require whole years. Exact birthday/payment-month timing, partial retirement years, spousal top-ups, and survivor benefits are not modeled. Spouse ages are entered independently and imply birth cohort from the same planning year as the primary. Existing scenarios missing spouse age require it when projecting future spouse income. Other income defaults to retirement start for older saved scenarios; distinct income streams with different starts must not be combined. Pre-retirement RMD projections are rejected rather than omitted silently.

`dev/test-consumer-snapshot.js`: 291 independent numeric checks plus invalid-domain, serialization, zero-volatility Monte Carlo and deep-link cases. `dev/test-phase2-cash-flow.js`: cash conservation and actual UI/chart/CSV/PDF payload reconciliation retained; initial balance expectation now correctly uses the beginning-of-retirement balance. Full top-level Phase 1–7 and Journey regressions passed. PHP syntax: 243 files. Older nested DB tests require isolated schemas; legacy shell-JavaScript tests require their `load` harness (covered by the top-level legacy harness).

The actual snapshot PDF was generated without HTTP and rendered for inspection. Inputs, timing context, model version, nominal USD basis and generated date are retained. Table sections start on separate pages; shared report context now resets text contrast and avoids advisor-only wording.

No statutory data changed. Consumer presentation remains distinct. No browser, live-site, AI-provider or Stripe request was made during verification.

## Batch 2 — consumer adapters and private sharing

Savings goals share a bounded monthly ledger, preserve final-month surplus, and use calendar month dates with end-of-month clipping. Compound interest shares the annuity ledger and preserves negative growth. Invalid/fractional horizons and malformed inputs are rejected; stale results are hidden. Monte Carlo succeeds when every requested withdrawal is funded, including exact final depletion, and clamps simulated return factors at zero rather than allowing negative assets. SS Gap no longer claims an unsupported probability of success. Future PAS withdrawals outside the horizon no longer get moved into the last modeled year.

Sharing defaults to the canonical calculator route. Including assumptions requires explicit opt-in; sensitive token/name/email keys and fragments are removed. Scenario loads validate calculator type and disable response caching. Consumer savings regression exercises actual adapters, independent monthly cash calculations, dates and zero-volatility Monte Carlo outcomes.

## Batch 3 — consumer Premium lifecycle and authorization

Consumer Checkout, confirmation and billing now use POST plus CSRF, canonical URLs, bounded Stripe timeouts and generic public errors. Checkout is serialized and persisted with provider idempotency keys, one introductory seven-day window per account, reused customers/open sessions, and protection against uncertain retries and concurrent subscriptions. A return must match the authenticated account and a locally recorded Checkout; the current subscription must use a configured consumer price. Return receipts are deduplicated and cannot replay a canceled entitlement.

The existing Phase 5 subscription processor and pure entitlement evaluator are reused with a separate consumer SQL store. Consumer, Journey and advisor records remain separate. Both configured signing secrets are verified on the existing root webhook; consumer lifecycle processing and Journey processing have separate receipts. Active/trial deadlines, scheduled cancellation, seven-day past-due grace, terminal cancellation, product changes and recovery are covered. The shared evaluator now respects a scheduled cancellation earlier than trial end. The shared ID validator accepts real cs_live_ and cs_test_ Checkout IDs; the earlier regex rejected these despite accepting simplified synthetic IDs. A stale users premium flag no longer authorizes paid Stripe features. Existing unlinked manual grants are preserved and date-limited when an end exists. Billing recovery remains available after Premium expires.

The additive migration creates consumer_subscriptions, consumer_checkout_sessions and consumer_webhook_events, with unique Stripe bindings and account foreign keys. No existing table is altered or user value rewritten. The actual migration and consumer SQL adapters passed 35 isolated-schema checks with synthetic provider callbacks. Legacy Journey DB tests now require a dedicated isolated schema and valid dated trial/account fixtures: 17 + 19 + 46 checks pass; they cannot accidentally mutate a real application schema. Full top-level regressions pass, including the shared advisor lifecycle.

Authentication retains rate limits, session rotation and CSRF; email input is trimmed/normalized and validated rather than silently removing malformed characters. Database failure details are hidden. Runtime Stripe customer actions and webhook delivery were not exercised against the provider; only injected provider fixtures were used.

## Batch 4 — final adapter, accessibility and export review

The actual snapshot form now preserves both spouses' explicit receiving/not-receiving choices through retirement selection and save/load. Selecting already retired no longer marks both spouses as receiving Social Security. Malformed values and fractional claim ages are passed to validation, not silently rounded or clamped. A saved retired primary age 66/spouse 56 scenario with both claiming at 70 pays zero Social Security now and targets $1,250,000 for $50,000 annual spending at 4%, regardless of later benefits or a pension starting at 73. Actual form serialization and reload reproduce identical projections.

Standalone consumer forms reject unsupported rates instead of silently clamping them; Monte Carlo amount parsing preserves negative signs and rejects malformed text. PAS uses editable total-fee assumptions and labels their illustrative nature, separates ending balance from withdrawals, preserves signed residual effects, and carries calendar start dates into reports. An independent two-year ledger reproduces a negative residual from differing percentage withdrawals instead of labeling every residual lost growth.

73 previously unnamed controls now have accessible names. All 27 catalog entries pass a static 450-check input-name, metadata and shared-footer audit. Result regions announce a concise update without reading entire projection tables. Existing dialog keyboard/focus/Escape protections remain; reduced-motion CSS is respected. Manual visual, screen-reader and device checks remain explicitly unperformed.

All seven server CSV endpoints now require bounded JSON (2 MiB, bounded row/nesting work), neutralize spreadsheet formulas, release session locks, disable caching, and include calculator/date/attribution/basis and available scalar assumptions. The eighth CSV is the snapshot client export. CSV contexts exclude identity/token fields. PDF shorthand background styles were replaced by supported background-color styles after a real rendered report showed invisible header text. Snapshot (five pages) and PAS (three pages) PDFs were generated and every page inspected locally; numerical payload/table/chart/CSV tests remain.

Existing shared Phase 1–7/Journey corrections were independently reconsidered through actual form adapters and known-answer tests rather than treated as inherently correct. Supported statutory tables and fixed model years remain unchanged. All 27 consumer calculators retain their consumer presentation; 16 advisor catalog entries reuse those actual engines, with presentation controlled by their separate product layer. Seven ancillary time-value-of-money calculators are outside the marketed 27-tool catalog and are included in the PHP numerical audit; IRR in the separate business module was also regression-tested because it uses shared math.

### Operations and limitations

The current release layout is active in production. Older deployment documents describe the historical pre-migration state; do not repeat that migration. Artifacts are built from committed files with locked Composer dependencies, hashed, linted and checked against CLI/Apache platforms. Activation uses the established release symlink/rollback process with SSH and isolated database fixtures, without any HTTP smoke tests. Prior releases remain available. The consumer migration backup was restored successfully into an isolated schema containing all 20 prior tables; original row counts and exact user values were preserved.

External Chart.js remains loaded from the existing CDN; no trusted local asset was available to vendor offline. Source-restricted CSP and complete CDN reproducibility are not certified. Baseline CSP, HTTPS/HSTS, host-only session cookies, resource denial and protected release contents are verified. Live browser visual checks, real Stripe customer/renewal/webhook delivery, current provider fee quotes, and historical credential rotation were not performed. No production Stripe, AI-provider, browser, GA4 or live website request was made by this audit. Existing unrelated Apache document-root/ServerName warnings remain outside the consumer application changes.
